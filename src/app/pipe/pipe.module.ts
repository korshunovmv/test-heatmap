import { NgModule }       from '@angular/core';
import { SecToMinSecPipe } from './sec-to-min-sec.pipe';


@NgModule({
  imports: [

  ],
  declarations: [
    SecToMinSecPipe,
  ],
  exports: [
    SecToMinSecPipe,
  ]

})

export class PipeModule {
}
